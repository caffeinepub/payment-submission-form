{
  "frontend/src/App.tsx": "Root application component with simple state-based routing between PaymentPage (default) and AdminPage, passing navigation callbacks as props.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend interactions — `useSubmitPayment` mutation now exposes `isActorReady` flag, guards execution with an `__actor_not_ready__` sentinel error that bypasses retry logic (retry callback skips retries for that specific error), and retries up to 2 times with exponential back-off for genuine transient failures; `useGetAllPayments` query unchanged with 3-retry exponential backoff gated on authenticated actor readiness.",
  "frontend/src/hooks/usePaymentForm.ts": "Custom hook managing all payment form state, field-level validation (email, card number, expiry, CVV, amount), auto-formatting for card number (spaces every 4 digits) and expiry date (MM/YY), and form reset logic.",
  "frontend/src/components/PaymentForm.tsx": "Clean light payment form that disables the submit button (showing \"Connecting…\" with a spinner) while the actor is initializing via the `isActorReady` flag from `useSubmitPayment`, shows a \"Connecting to payment service…\" banner during initialization, prevents form submission until the actor is ready, and otherwise retains all existing field layout, validation, success screen, and error display logic.",
  "frontend/src/pages/PaymentPage.tsx": "Clean light fintech payment page with white header, amber/gold logo icon, SSL secure badge, trust badges, and PaymentForm — all POS dark backgrounds, LED indicators, scan-line overlays, and monospaced branding removed.",
  "frontend/src/index.css": "Clean light fintech theme with warm neutral OKLCH palette, amber/gold accent tokens, DM Sans font, and component classes for `.payment-card`, `.payment-input`, `.payment-input-mono`, `.btn-primary`, and `.section-label` — all POS terminal dark styles, LED indicators, scanlines, and Share Tech Mono removed.",
  "frontend/tailwind.config.js": "Tailwind config with DM Sans as primary font, warm neutral and amber/gold OKLCH color tokens, clean card/amber-glow box shadows — all POS terminal device palette, pos-green, pos-amber, and Share Tech Mono tokens removed.",
  "No files were modified — the existing frontend implementation already satisfies all acceptance criteria": "`useQueries.ts` calls `actor.submitPayment` with all required parameters, `PaymentForm.tsx` disables the submit button during pending state, shows a success confirmation screen on completion, and resets the form fields after successful submission.",
  "frontend/src/pages/AdminPage.tsx": "Clean light admin page with fixed loading/error state logic — shows skeleton with retry progress indicator while React Query retries (failureCount < 4), only shows the \"Failed to load payments\" error after all 4 attempts are exhausted, and invalidates the payments query via `useEffect` when the authenticated actor becomes ready after login.",
  "frontend/src/components/AdminLoginGate.tsx": "Clean light admin login gate with white card surface, amber/gold lock icon, standard readable typography, and smooth amber login button — all POS dark device body, LED indicators, and monospaced terminal styling removed."
}