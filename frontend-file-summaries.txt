{
  "frontend/src/App.tsx": "Root application component with simple state-based routing between `PaymentPage` (default) and `AdminPage`, passing the correct `onNavigateToPayment` prop to `AdminPage`.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend interactions — derives `actorReady` locally from `!!actor && !isActorFetching` (avoiding the missing `actorReady` property on `useActor`'s return type); `useGetAllPayments` is gated on `actorReady && isAuthenticated` with retry:3 and exponential back-off; `useSubmitPayment` returns the mutation plus the locally-derived `actorReady` flag with retry:2 and exponential back-off.",
  "frontend/src/hooks/usePaymentForm.ts": "Custom hook managing all payment form state, field-level validation (email, card number, expiry, CVV, amount), auto-formatting for card number (spaces every 4 digits) and expiry date (MM/YY), and form reset logic.",
  "frontend/src/components/PaymentForm.tsx": "Payment form that adapts `usePaymentForm`'s `(name, value)` handlers to native `ChangeEvent` handlers via `makeChangeHandler`/`makeBlurHandler` wrappers, disables the submit button with a spinner while `actorReady` is false, suppresses transient \"not ready\" errors, and shows a success screen with form reset on successful submission.",
  "frontend/src/pages/PaymentPage.tsx": "Clean light fintech payment page with white header, amber/gold logo icon, SSL secure badge, trust badges, and PaymentForm — all POS dark backgrounds, LED indicators, scan-line overlays, and monospaced branding removed.",
  "frontend/src/index.css": "Clean light fintech theme with warm neutral OKLCH palette, amber/gold accent tokens, DM Sans font, and component classes for `.payment-card`, `.payment-input`, `.payment-input-mono`, `.btn-primary`, and `.section-label` — all POS terminal dark styles, LED indicators, scanlines, and Share Tech Mono removed.",
  "frontend/tailwind.config.js": "Tailwind config with DM Sans as primary font, warm neutral and amber/gold OKLCH color tokens, clean card/amber-glow box shadows — all POS terminal device palette, pos-green, pos-amber, and Share Tech Mono tokens removed.",
  "No files were modified — the existing frontend implementation already satisfies all acceptance criteria": "`useQueries.ts` calls `actor.submitPayment` with all required parameters, `PaymentForm.tsx` disables the submit button during pending state, shows a success confirmation screen on completion, and resets the form fields after successful submission.",
  "frontend/src/pages/AdminPage.tsx": "Admin dashboard that derives `actorReady` locally from `!!actor && !isActorFetching` (no longer destructures the missing property from `useActor`), shows a loading spinner while the actor or query is initializing/retrying, displays the error UI only after all React Query retries are exhausted, and renders all payment rows with masked card numbers and all required columns.",
  "frontend/src/components/AdminLoginGate.tsx": "Clean light admin login gate with white card surface, amber/gold lock icon, standard readable typography, and smooth amber login button — all POS dark device body, LED indicators, and monospaced terminal styling removed.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor using the current identity, exposes `actorReady` (true only after `isInitializing` is false and the actor is fully instantiated without any in-flight fetch), uses a `useRef` to track the previous actor so the invalidation effect only fires when the actor genuinely changes (not on every render), preventing the race condition that caused the payments table to never load."
}